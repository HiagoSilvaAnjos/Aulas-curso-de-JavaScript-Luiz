// TODO: Escreva uma função chamada paisagem que recebe dois argumentos, largura e altura de uma imagem (Number) e Retorne true se a imagem estivr no modo paisagem.

const landscape = (largura, altura) =>largura >= altura;
console.log(landscape(1080, 1920))